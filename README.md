# 曼城世界杯赛程

公开网页版本。GitHub Pages 读取 `docs/schedule-live.json`。

当前 GitHub CLI token 没有 `workflow` 权限，所以自动定时更新 workflow 暂未提交。可以先手动刷新数据后推送；后续刷新 GitHub CLI 的 `workflow` 权限后再加入定时更新。

本地刷新数据：

```bash
npm run update:data
```
